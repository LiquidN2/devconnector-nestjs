import styled from 'styled-components';
import { ReactComponent as SvgComponent } from '../../assets/img/icons/code.svg';

const Logo = styled(SvgComponent)`
  height: 4rem;
  width: 4rem;
  fill: var(--color-primary);
`;

export default Logo;
